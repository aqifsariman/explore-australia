<%- include('header') -%>
<div class="jumbotron d-flex align-items-center justify-content-center attractions-bg">
<form class="row g-3 needs-validation" action="/country-search" method="POST" novalidate>
<h1 class="text-center fw-bolder text-light">Find The Best Flight</h1>
<div class="row mt-3">
     <div class="col-md-12 mt-3">
    <div>
    </div>
    <label for="country" class="form-label text-light">Country</label>
    <select class="form-select border-top-0 border-end-0 border-start-0" aria-label="Default select example" name="country">
       <option selected>Choose Origin Country</option>
      <% sortedCountries.forEach((country) =>{ %>
      <option value="<%= country %>"><%= country %></option>
     <% }); %>
    </select>
     <div class="invalid-feedback">
      Please input valid data.
    </div>
    </div>
   <!-- <div class="col-md-12 mt-3">
    <div>
    <label for="cityId" class="form-label text-light">Destination</label>
    </div>
    <select class="form-select border-top-0 border-end-0 border-start-0" aria-label="Default select example" name="cityId">
       <option selected>Choose Destination</option>
      <%# listOfCities.forEach((city) =>{ %>
      <option value="<%#= city.id %>"><%#= city.name %></option>
     <%# }); %>
    </select>
     <div class="invalid-feedback">
      Please input valid data.
    </div>
    </div> -->
    <!-- <div class="valid-feedback">
      Looks good!
    </div> -->
  </div>
  <div class="mt-4 d-flex justify-content-center ">
    <button class="btn btn-outline-light w-50" type="submit">Next </button>
  </div>
</form>
  </div>

  <script>// Example starter JavaScript for disabling form submissions if there are invalid fields
(function () {
  'use strict'

  // Fetch all the forms we want to apply custom Bootstrap validation styles to
  let forms = document.querySelectorAll('.needs-validation')

  // Loop over them and prevent submission
  Array.prototype.slice.call(forms)
    .forEach(function (form) {
      form.addEventListener('submit', function (event) {
        if (!form.checkValidity()) {
          event.preventDefault()
          event.stopPropagation()
        }

        form.classList.add('was-validated')
      }, false)
    })
})();

// let dateTime = document.getElementById("date").value;
// let today = new Date().toISOString().split('T')[0];
// document.getElementById("dateTime").setAttribute('max', today);
</script>


<%- include('footer') -%>
